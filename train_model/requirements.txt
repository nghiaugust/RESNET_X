# CPU-only PyTorch can make training fall back to CPU. For NVIDIA GPU training,
# install a CUDA-enabled PyTorch wheel first, for example:
#   pip install -r requirements-gpu-cu128.txt
torch
torchvision
scikit-learn
numpy
pandas
matplotlib
Pillow
PyYAML
tqdm
tensorboard
joblib
